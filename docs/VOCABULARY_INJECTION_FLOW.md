# LaLea 額外詞彙注入流程草案

> 狀態：待使用者審查  
> 最後更新：2026-08-21  
> 範圍：MVP 手動加入單一單字或片語

## 1. 目的

讓使用者遇到課程外的單字或片語時，可以先快速保存，再由系統與操作者逐步補齊成可學習、可複習且來源可追蹤的內容。

MVP 的設計重點是「先收下、不遺失」，不要求使用者在加入當下填完所有教材欄位。

## 2. MVP 範圍

### 包含

- 手動輸入單一單字或片語。
- 指定目標語言；預設沿用目前學習路徑。
- 選填原句、中文理解、來源連結與個人備註。
- 搜尋既有詞條與可能重複項目。
- 已有詞條可直接加入個人詞彙庫。
- 資料不足時建立待處理項目。
- 透過操作者的 Codex／Claude 訂閱工作流協助產生草稿。
- 結構驗證與人工確認後發布為可複習內容。

### 不包含

- 任意文件或大量詞彙批次匯入。
- 自動爬取網頁全文。
- 未經允許下載 YouTube 字幕。
- AI 產生後自動發布。
- 直接從公開網站請求啟動 Codex／Claude CLI。
- 讓使用者自由輸入答案的開放式測驗。

## 3. 使用者快速加入流程

```text
輸入單字或片語
  → 基本正規化與搜尋
  → 找到既有詞義：選擇詞義並加入個人詞彙庫
  → 只有相似項目：讓使用者確認是否為同一用法
  → 沒有合適資料：先保存為待補齊項目
  → 操作者補齊並審核
  → 通知使用者可加入學習
  → 進入首次學習，不直接當作已掌握
```

## 4. 快速加入表單

### 必填

- 原文，例如 `follow up`。
- 目標語言，例如英文。

### 選填

- 遇到它的完整原句。
- 使用者目前理解的意思。
- 來源類型：自己輸入、課程、YouTube、網頁、文件或其他。
- 來源連結或識別碼。
- 個人備註與標籤。

主要按鈕為「加入詞彙庫」。進階欄位預設收合，讓最短流程只需輸入文字並提交。

## 5. 正規化與重複判定

提交後先執行不改變原始資料的標準化：

- 去除前後多餘空白。
- 保留使用者輸入的原始寫法。
- 另存適合搜尋的正規化形式。
- 依語言規則處理大小寫、標點與空格。
- 不假設所有語言都有英文式大小寫或空格。

搜尋順序：

1. 同一語言、完全相同寫法。
2. 同一語言、正規化後相同。
3. 已知詞形變化或替代拼法。
4. 僅列為可能相似，不自動合併。

重複判定以「詞條＋詞義」為核心。相同拼法可能有多個詞義，不應只因文字相同就合併學習內容。

## 6. 找到既有詞條

若已有一個或多個經審核詞義，顯示：

- 詞性與簡短解釋。
- 代表例句。
- 使用情境與正式程度。
- 是否已在個人詞彙庫中。

使用者選擇正確詞義後：

- 建立個人的詞義學習紀錄。
- 保存這次遇到的原句、來源與備註，作為個人上下文。
- 若原本已加入，更新個人上下文，不建立重複學習紀錄。
- 排入「首次學習」，不立刻排成一般到期複習。

## 7. 沒有合適詞條

系統先建立一筆待處理項目，狀態為：

`captured → needs_enrichment → needs_review → approved → ready_to_learn`

若被判定為無效、重複或不適合，則可進入：

`rejected` 或 `merged`

使用者提交後立即看到「已保存，等待補齊」，不需要停留等待 AI 處理。

## 8. AI 輔助補齊工作流

### 網站產生的任務包

- 原始文字與語言。
- 原句、來源與使用者理解。
- 可能的既有詞條。
- 需要產出的欄位與固定 JSON schema 版本。
- 禁止推測或需要標記不確定的規則。

### 建議產出

- 詞條類型：單字、片語、慣用語或句型。
- 詞性或語法功能。
- 候選詞義。
- 繁體中文解釋。
- 常見搭配。
- 一般商務英語例句與翻譯。
- 語氣、正式程度與使用提醒。
- 容易混淆的說法。
- 建議的選擇、排序、句塊與聽力題素材。
- 不確定項目與需要人工核實之處。

操作者手動匯出任務包，在已登入的 Codex 或 Claude 中處理，再把結果匯回。網站只接受符合 schema 的資料；CLI 不由公開請求直接觸發。

## 9. 結構驗證與人工審核

### 自動結構驗證

- 必填欄位存在。
- 語言識別碼有效。
- 列舉值與 schema 版本有效。
- 原文與翻譯未被放入錯誤語言欄位。
- 例句、題目與答案引用的詞義存在。
- 來源欄位未遺失。

### 人工審核

- 詞義是否符合使用者提供的原句。
- 中文解釋是否自然且不過度直譯。
- 例句是否自然、正確且適合一般商務情境。
- 正式程度與語氣標記是否合理。
- 是否與既有詞義重複。
- 題目是否有明確答案，不依賴開放式評分。
- AI 未捏造字典來源、引用或影片內容。

只有通過人工確認的內容才能標記為 `approved`。

## 10. 進入學習與複習

新注入詞彙不直接成為到期複習項目，而是先進入首次學習：

1. 情境與詞義介紹。
2. 閱讀辨識。
3. 聽力辨識；若尚無音訊可延後。
4. 使用選擇、排序或固定句塊進行主動提取。
5. 使用者自評：忘記／困難／掌握／太容易。
6. 根據結果建立閱讀、聽力與主動表達的獨立複習排程。

若只有最低限度資料，可以先進行閱讀辨識；欠缺的聽力或情境練習不得假裝已完成。

## 11. 使用者可見狀態

詞彙庫中的個人項目顯示簡單狀態：

- 待補齊
- 待確認
- 可開始學習
- 學習中
- 已暫時忽略

內部工作流可使用更細的狀態，但不把技術狀態全部暴露給一般使用者。

使用者可以：

- 修改個人備註與標籤。
- 補充原句或來源。
- 取消待處理項目。
- 將項目暫時忽略或恢復。
- 回報詞義或例句有問題。

已發布的共用詞義不能由一般使用者直接覆寫；修正應建立新版或審核請求，避免影響其他使用者。

## 12. 建議資料邊界

| 類型 | 用途 |
| --- | --- |
| 共用詞條與詞義 | 儲存經審核、可供多位使用者重用的語言資料 |
| 個人詞彙項目 | 連結使用者、學習路徑、詞義與個人狀態 |
| 個人上下文 | 保存使用者遇到詞彙的原句、來源、備註與標籤 |
| 注入任務 | 追蹤補齊、驗證、審核、合併或拒絕狀態 |
| 內容版本 | 保留教材修改紀錄，避免進行中的複習引用內容突然改變 |
| 學習事件 | 記錄首次學習與每次複習結果 |

## 13. 隱私與來源

- 使用者貼上的句子可能含有公司、客戶或專案機密，送往第三方訂閱 AI 前必須明確提示。
- 任務包應允許操作者先移除姓名、公司名、料號與其他敏感資訊。
- 個人原句預設不自動成為所有使用者共用教材。
- 每筆教材保留來源類型、產生方式、審核者與版本。
- URL 只是來源線索，不代表內容具有可重製或重新發布的授權。

## 14. 例子

使用者輸入：

```text
原文：hold off on
原句：Let's hold off on sending the report until the numbers are confirmed.
我的理解：先不要做
來源：個人工作筆記
```

若已有經審核詞義「暫緩某項行動」，使用者確認後直接加入個人詞彙庫，原句只保存在個人上下文中。若沒有，系統保存待補齊項目，由操作者產生草稿並審核後，再通知使用者開始首次學習。

## 15. 第一輪需要驗證

- 最短加入流程是否能在 10 秒左右完成？
- 要求填寫原句是否應維持選填？
- 相同拼法、多個詞義的選擇是否容易理解？
- 「保存」與「正式進入複習」的時間差是否會造成困惑？
- 人工 AI 工作流處理少量個人詞彙是否可接受？
- 哪些欄位是首次學習前真正必要，哪些可以稍後補充？
- 敏感工作內容的提示與去識別流程是否足夠？
