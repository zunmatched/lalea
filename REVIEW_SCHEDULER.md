# LaLea MVP 複習排程器規格

> 狀態：開發基線  
> 最後更新：2026-08-21  
> 決策：MVP 使用簡化、可解釋的間隔規則，保留未來替換 FSRS 類模型的能力

## 1. 原則

- 閱讀辨識、聽力辨識與主動提取分開排程。
- 到期複習優先於新內容。
- 排程結果由可替換介面產生，不寫死在頁面或題型中。
- 所有作答、自評與排程前後狀態都保存為不可變事件。
- 被動播放只記錄曝露，不直接更新掌握度。
- 「想起來了」為低權重自評；正式題目結果權重較高。
- MVP 不宣稱使用 FSRS，也不顯示虛假的精準記憶率。

## 2. 首次學習

新詞義先完成至少一個首次學習單元，再建立複習排程。

首次學習後的建議間隔：

| 結果 | 下次複習 |
| --- | --- |
| 忘記／答錯 | 10 分鐘後或當日稍後 |
| 困難 | 1 天後 |
| 掌握 | 2 天後 |
| 太容易 | 4 天後 |

若當日不再開啟，10 分鐘後的項目在下次使用時優先出現，不因錯過精確時間而判定失敗。

## 3. 後續複習

每個掌握度面向保存目前間隔天數。正式題目完成後：

| 結果 | 新間隔 |
| --- | --- |
| 忘記／答錯 | 重設為當日稍後或 1 天 |
| 困難 | 舊間隔 × 1.2，至少 1 天 |
| 掌握 | 舊間隔 × 2.0 |
| 太容易 | 舊間隔 × 3.0 |

第一版上限暫定 90 天，避免未經長期驗證就排得過遠。計算結果取整天，但短於一天的首次重試可使用分鐘或小時。

## 4. 正確性與自評的組合

- 答錯時，即使選擇「掌握」，仍以錯誤結果為主。
- 答對但自評「困難」，使用困難間隔。
- 答對且自評「忘記」視為使用者表示可能是猜中，安排較早複習。
- 自評缺失時，答對預設為掌握、答錯預設為忘記。
- 純聽的「不熟」可提前正式複習，但不直接建立一次答錯事件。

## 5. 每日佇列

建議排序：

1. 已逾期且曾答錯的項目。
2. 今日到期的主動提取。
3. 今日到期的聽力辨識。
4. 今日到期的閱讀辨識。
5. 最近標記不熟的內容。
6. 新內容。

每日預設新增 3–5 個詞義；若到期複習量過高，自動減少或停止新增。

## 6. 時間與環境配對

- 1 分鐘：優先一至三個到期選擇題或句塊題。
- 3 分鐘：一個完整微型單元。
- 5 分鐘：到期複習加少量新內容。
- 靜音：不安排必要聽力題。
- 可聽：可混合聽力與其他題型。
- 純聽：只建立播放清單，不把播放當作正式複習完成。

## 7. 排程器介面

概念輸入：

- 現有掌握度狀態。
- 掌握度面向。
- 題目結果。
- 使用者自評。
- 作答時間與輔助使用情況。
- 排程器版本。

概念輸出：

- 新狀態。
- 新間隔。
- 下次複習時間。
- 變更原因代碼。
- 排程器版本。

排程器不得直接改寫原始作答事件。

## 8. 未來替換條件

累積足夠真實複習資料後，再評估 FSRS 類模型。替換前需要：

- 保留完整時間戳、結果、自評與歷史排程。
- 以歷史事件回放比較新舊排程。
- 對不同掌握度面向分開校準。
- 可回退到前一版本。
- 不因更換演算法刪除或改寫歷史。

## 9. 驗收條件

- 三種掌握度不互相覆蓋。
- 答錯後會比答對更早複習。
- 到期項目優先於新內容。
- 同一事件重送不會重複更新排程。
- 排程結果能解釋使用了哪一條規則。
- 更換排程器版本不需要修改教材資料。

